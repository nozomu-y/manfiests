apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitea
  namespace: gitea
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`gitea.local`)
      kind: Rule
      services:
        - name: gitea
          port: 3000
